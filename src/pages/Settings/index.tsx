import React,{useCallback,useEffect,useState}from'react';import axios from'axios';import Editor from'react-simple-code-editor';import Prism from'prismjs';import'prismjs/components/prism-python';import{MdAdd,MdHistory,MdPlayArrow,MdRefresh,MdSettings}from'react-icons/md';import{URL_API}from'../../repositories/baseAPI';import{Container}from'./styles';
interface Job{name:string;enabled:boolean;description?:string;last_status?:number;last_run_date?:number;last_run_duration?:number;can_run:boolean}interface History{step_name:string;run_status:number;run_date:number;message:string}const state=(v?:number)=>v===1?'Sucesso':v===0?'Falhou':v===4?'Em andamento':'Sem execução';const blank={name:'',description:'',apiDir:'C:\\Dev\\Gaspar Solutions\\GasparAPI',pythonCode:'',enabled:true,schedule:{enabled:true,frequency:'daily',time:'00:00',weekdays:127}};
export default function Settings(){const[jobs,setJobs]=useState<Job[]>([]),[job,setJob]=useState<Job|null>(null),[history,setHistory]=useState<History[]>([]),[form,setForm]=useState<any>(blank),[edit,setEdit]=useState(false),[message,setMessage]=useState('');const load=useCallback(async()=>{try{setJobs((await axios.get(`${URL_API}/automations`)).data)}catch{setMessage('Não foi possível carregar as automações.')}},[]);useEffect(()=>{load()},[load]);const select=async(j:Job)=>{setJob(j);setEdit(false);setHistory((await axios.get(`${URL_API}/automations/${encodeURIComponent(j.name)}/history`)).data)};const open=async(j?:Job)=>{if(!j){setJob(null);setForm(blank);setEdit(true);return}try{setForm((await axios.get(`${URL_API}/automations/${encodeURIComponent(j.name)}/definition`)).data);setEdit(true)}catch(e:any){setMessage(e.response?.data?.message||'Este job não pode ser editado por esta tela.')}};const save=async()=>{try{const{name,...body}=form;const r=job?await axios.put(`${URL_API}/automations/${encodeURIComponent(job.name)}/definition`,body):await axios.post(`${URL_API}/automations`,form);setMessage(r.data.message);setEdit(false);load()}catch(e:any){setMessage(e.response?.data?.message||'Não foi possível salvar o job.')}};const toggle=async()=>{if(!job)return;const r=await axios.patch(`${URL_API}/automations/${encodeURIComponent(job.name)}/status`,{enabled:!job.enabled});setMessage(r.data.message);setJob({...job,enabled:!job.enabled});load()};return <Container><header><div><span><MdSettings/> CONFIGURAÇÕES</span><h1>Automações</h1><p>Crie rotinas Python, altere agendas e acompanhe suas execuções.</p></div><div className="header-actions"><button onClick={()=>open()}><MdAdd/> Novo job</button><button onClick={load}><MdRefresh/> Atualizar</button></div></header>{message&&<div className="notice">{message}</div>}<main><section className="job-list"><div className="section-title"><h2>Rotinas</h2></div>{jobs.map(j=><button className={`job ${job?.name===j.name?'selected':''}`} key={j.name} onClick={()=>select(j)}><span className={`dot ${state(j.last_status).replaceAll(' ','-')}`}/><div><strong>{j.name}</strong><small>{j.description||'Rotina automática'}</small></div><em>{state(j.last_status)}</em></button>)}</section><section className="details">{edit?<div className="editor"><h2>{job?`Editar ${job.name}`:'Novo job'}</h2><label>Nome<input disabled={!!job} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Descrição<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Pasta da API<input value={form.apiDir||''} onChange={e=>setForm({...form,apiDir:e.target.value})}/></label><div className="schedule"><label>Frequência<select value={form.schedule.frequency} onChange={e=>setForm({...form,schedule:{...form.schedule,frequency:e.target.value}})}><option value="daily">Diariamente</option><option value="weekly">Semanalmente</option></select></label><label>Horário<input type="time" value={form.schedule.time} onChange={e=>setForm({...form,schedule:{...form.schedule,time:e.target.value}})}/></label></div><label>Código Python<Editor value={form.pythonCode} onValueChange={pythonCode=>setForm({...form,pythonCode})} highlight={code=>Prism.highlight(code,Prism.languages.python,'python')} padding={14} textareaId="python-editor" className="python-editor" style={{fontFamily:'Consolas, monospace',fontSize:13,minHeight:360}}/></label><div className="editor-actions"><button onClick={()=>setEdit(false)}>Cancelar</button><button className="run" onClick={save}>Salvar job</button></div></div>:job?<><div className="detail-head"><div><span>DETALHES DA AUTOMAÇÃO</span><h2>{job.name}</h2><p>{job.description||'Sem descrição cadastrada.'}</p></div><div className="header-actions"><button onClick={toggle}>{job.enabled?'Desativar':'Ativar'}</button><button onClick={()=>open(job)}>Editar</button>{job.can_run&&<button className="run" onClick={()=>axios.post(`${URL_API}/automations/${encodeURIComponent(job.name)}/start`).then(r=>setMessage(r.data.message))}><MdPlayArrow/> Executar agora</button>}</div></div><div className="meta"><div><small>Status</small><b>{job.enabled?state(job.last_status):'Desativado'}</b></div><div><small>Última execução</small><b>{job.last_run_date||'—'}</b></div><div><small>Duração</small><b>{job.last_run_duration||'—'}</b></div></div><h3><MdHistory/> Histórico</h3><div className="history">{history.map((h,i)=><article key={i}><b>{state(h.run_status)}</b><span>{h.step_name} · {h.run_date}</span><p>{h.message}</p></article>)}</div></>:<div className="empty"><MdSettings/><h2>Selecione uma automação</h2></div>}</section></main></Container>}
